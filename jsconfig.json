{
  "compilerOptions": {
    "baseUrl": "./", // Ensures paths are relative to the project root
    "paths": {
      "@/*": ["src/*"] // Aliases everything under `src` to `@/`
    }
  },
  "include": ["src/**/*"] // Includes all files under `src`
}
